[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cxr-intel"
version = "0.1.0"
description = "Multi-Modal Chest X-Ray Intelligence System"
readme = "README.md"
requires-python = ">=3.9"
